export const environment = {
  production: true,
  // Reemplaza esto con la URL real de tu backend en Render
  // (ej. https://webtic-backend-api.onrender.com) antes de desplegar a Vercel.
  apiUrl: 'https://TU-BACKEND.onrender.com'
};
